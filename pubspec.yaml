name: my_wallet
description: "My Wallet — shaxsiy va oilaviy byudjet (Android)"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

# Pub workspace: ilova + sof Dart domen paketi bitta resolyutsiyada.
workspace:
  - packages/wallet_domain

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^3.4.3
  intl: any
  meta: ^1.19.0
  wallet_domain:
    path: packages/wallet_domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^11.0.0

flutter:
  uses-material-design: true
  generate: true
