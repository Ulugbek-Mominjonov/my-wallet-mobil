<?xml version="1.0" encoding="utf-8"?>
<!-- E33-T01: bosh ekran vidjeti — joriy oy qoldig'i, "kuniga ≈ X" va
     "＋" tugmasi. Qiymatlar ilovadan keladi (home_widget); maxfiylik
     rejimida summalar "•••" bo'lib yoziladi (BR-212). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/home_widget_background">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="#FFFFFFFF"
        android:alpha="0.8"
        android:maxLines="1"
        android:ellipsize="end"
        tools:ignore="HardcodedText"
        xmlns:tools="http://schemas.android.com/tools" />

    <TextView
        android:id="@+id/widget_balance"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#FFFFFFFF"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/widget_per_day"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textSize="13sp"
        android:textColor="#FFFFFFFF"
        android:alpha="0.9"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/widget_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:paddingHorizontal="16dp"
        android:paddingVertical="6dp"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#FF4F46E5"
        android:background="@drawable/home_widget_button" />
</LinearLayout>
