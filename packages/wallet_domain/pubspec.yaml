name: wallet_domain
description: "My Wallet biznes qoidalari — sof Dart (Flutter va Supabase'siz)."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  collection: ^1.19.1
  meta: ^1.19.0

dev_dependencies:
  test: ^1.26.0
  very_good_analysis: ^11.0.0
