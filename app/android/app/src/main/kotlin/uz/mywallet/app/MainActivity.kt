package uz.mywallet.app

import io.flutter.embedding.android.FlutterFragmentActivity

/**
 * `FlutterActivity` EMAS, `FlutterFragmentActivity`.
 *
 * `local_auth` biometrik oynani `FragmentActivity` ustida ko'rsatadi —
 * oddiy `FlutterActivity` bilan qulf ekrani ochilganda ilova qulaydi.
 */
class MainActivity : FlutterFragmentActivity()
